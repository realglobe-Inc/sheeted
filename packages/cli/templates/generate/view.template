import { View } from '@sheeted/core'

import { <%= entityNamePascal %>Entity } from './<%= entityNameParam %>.entity'

export const <%= entityNamePascal %>View: View<<%= entityNamePascal %>Entity> = {
  title: '<%= entityNamePascal %>',
  display: (entity) => entity.name,
  columns: [
    {
      field: 'name',
      title: 'NAME',
    },
  ],
}
