import { compileModel } from '@sheeted/mongoose'

import { <%= entityNamePascal %>Schema } from './<%= entityNameKebab %>.schema'
import { <%= entityNamePascal %>Entity } from './<%= entityNameKebab %>.entity'

// TODO: Pass model name to the first argument. The model name must be the same as sheet name
export const <%= entityNamePascal %>Model = compileModel<<%= entityNamePascal %>Entity>('<%= entityNamePascal %>', <%= entityNamePascal %>Schema)
