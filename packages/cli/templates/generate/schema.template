import { Types, Schema } from '@sheeted/core'

import { <%= entityNamePascal %>Entity } from './<%= entityNameParam %>.entity'

export const <%= entityNamePascal %>Schema: Schema<<%= entityNamePascal %>Entity> = {
  name: {
    type: Types.Text,
    searchable: true,
    optional: true,
  },
}
